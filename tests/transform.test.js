import ink from '../rollup-plugin-ink';

describe('Transform to ES module', () => {
  test('should has proper name', async () => {
    // const inkPlugin = ink();

    // const result = await inkPlugin.transform('', './test.ink');
    expect(true).toBe(true);
  });
});
